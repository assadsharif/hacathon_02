# Local Development with Docker PostgreSQL
# Copy this to .env.local to use local PostgreSQL instead of Neon

DEBUG=True
ENVIRONMENT=development

# Local PostgreSQL (from docker-compose.yml)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/todos_dev

# Note: When using local PostgreSQL, you don't need sslmode=require
# The local database doesn't require SSL connections
