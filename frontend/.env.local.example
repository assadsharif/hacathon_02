# Frontend Environment Variables Configuration
# [Task]: AUTH-C8 - Environment variable configuration
#
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to version control (already in .gitignore)

# =============================================================================
# Database Configuration (Neon PostgreSQL)
# =============================================================================
# Better Auth requires database access for user accounts and sessions
# This should point to the SAME Neon database as the backend
#
# Get your DATABASE_URL from Neon Console: https://console.neon.tech
# Format: postgresql://<user>:<password>@<host>/<database>?sslmode=require
#
# Example:
# DATABASE_URL=postgresql://neondb_owner:password@ep-example-123.aws.neon.tech/neondb?sslmode=require
DATABASE_URL=your_neon_database_url_here

# =============================================================================
# Authentication Configuration (JWT)
# =============================================================================
# JWT secret key for token signing and verification
# MUST match the JWT_SECRET in the backend .env file
#
# CRITICAL: This MUST be the same secret used by the backend FastAPI app.
# Better Auth signs tokens with this secret, backend verifies with same secret.
#
# Minimum 256-bit (32 characters) for HS256 algorithm
# Generate a secure random secret: openssl rand -hex 32
#
# Example:
# JWT_SECRET=93457b5f5d59fd9d65726648e22a4e28
JWT_SECRET=your_jwt_secret_here

# =============================================================================
# Application Configuration
# =============================================================================
# Base URL for the Next.js application
# Used by Better Auth for callbacks and redirects
#
# Development: http://localhost:3000
# Production: Your deployed frontend URL (e.g., https://myapp.vercel.app)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Backend API URL
# URL where the FastAPI backend is running
#
# Development: http://localhost:8000
# Production: Your deployed backend URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# =============================================================================
# Better Auth Configuration (Optional)
# =============================================================================
# Additional Better Auth configuration options
# Uncomment and configure as needed for production

# Trust proxy headers (set to "true" in production with reverse proxy)
# BETTER_AUTH_TRUST_HOST=false

# Session cookie name (default: "better-auth.session_token")
# BETTER_AUTH_SESSION_COOKIE_NAME=better-auth.session_token

# =============================================================================
# Notes
# =============================================================================
# - All NEXT_PUBLIC_* variables are exposed to the browser
# - Keep JWT_SECRET and DATABASE_URL secure (server-side only)
# - Ensure JWT_SECRET matches between frontend and backend
# - Use the same DATABASE_URL for both frontend and backend
