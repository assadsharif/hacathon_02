# FastAPI Backend Dependencies - Phase II

# FastAPI framework
fastapi==0.109.0

# ASGI server for running FastAPI
uvicorn[standard]==0.27.0

# SQLModel for ORM (combines SQLAlchemy + Pydantic)
sqlmodel==0.0.14

# PostgreSQL adapter for Python
psycopg2-binary==2.9.9

# Environment variable management
python-dotenv==1.0.0

# Additional dependencies
pydantic==2.5.3
pydantic-settings==2.1.0

# Email validation for Pydantic EmailStr
email-validator==2.1.0

# Testing dependencies
requests==2.31.0

# [Task]: AUTH-B1 - Authentication dependencies
# JWT encoding/decoding for token verification
python-jose[cryptography]==3.3.0

# Password hashing (if not using Better Auth's hashing)
passlib[bcrypt]==1.7.4

# Production server
gunicorn==21.2.0
